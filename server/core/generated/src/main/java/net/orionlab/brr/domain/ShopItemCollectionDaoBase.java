// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: hibernate/SpringHibernateDaoBase.vsl in andromda-spring-cartridge.
//
package net.orionlab.brr.domain;

import java.security.Principal;
import java.util.Collection;
import java.util.Date;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import net.orionlab.brr.PrincipalStore;
import net.orionlab.brr.PropertySearch;
import net.orionlab.brr.Search;
import net.orionlab.brr.SearchParameter;
import org.andromda.spring.PaginationResult;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>ShopItemCollection</code>.
 * </p>
 *
 * @see ShopItemCollection
 */
public abstract class ShopItemCollectionDaoBase
    extends HibernateDaoSupport
    implements ShopItemCollectionDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(ShopItemCollectionImpl.class, id);
        return transformEntity(transform, (ShopItemCollection)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public ShopItemCollection get(Long id)
    {
        return (ShopItemCollection)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(ShopItemCollectionImpl.class, id);
        return transformEntity(transform, (ShopItemCollection)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public ShopItemCollection load(Long id)
    {
        return (ShopItemCollection)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<ShopItemCollection> loadAll()
    {
        return (Collection<ShopItemCollection>) this.loadAll(ShopItemCollectionDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(ShopItemCollectionDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(ShopItemCollectionImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public ShopItemCollection create(ShopItemCollection shopItemCollection)
    {
        return (ShopItemCollection)this.create(ShopItemCollectionDao.TRANSFORM_NONE, shopItemCollection);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final ShopItemCollection shopItemCollection)
    {
        if (shopItemCollection == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.create - 'shopItemCollection' can not be null");
        }
        this.getHibernateTemplate().save(shopItemCollection);
        return this.transformEntity(transform, shopItemCollection);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<ShopItemCollection> create(final Collection<ShopItemCollection> entities)
    {
        return (Collection<ShopItemCollection>) create(ShopItemCollectionDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<ShopItemCollection> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (ShopItemCollection entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public ShopItemCollection create(
        String name,
        String imageName,
        Integer cost,
        Integer orderNum,
        Date dateCreate,
        Date dateEdit,
        Boolean isArchive)
    {
        return (ShopItemCollection)this.create(ShopItemCollectionDao.TRANSFORM_NONE, name, imageName, cost, orderNum, dateCreate, dateEdit, isArchive);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        String name,
        String imageName,
        Integer cost,
        Integer orderNum,
        Date dateCreate,
        Date dateEdit,
        Boolean isArchive)
    {
        ShopItemCollection entity = new ShopItemCollectionImpl();
        entity.setName(name);
        entity.setImageName(imageName);
        entity.setCost(cost);
        entity.setOrderNum(orderNum);
        entity.setDateCreate(dateCreate);
        entity.setDateEdit(dateEdit);
        entity.setIsArchive(isArchive);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(ShopItemCollection shopItemCollection)
    {
        if (shopItemCollection == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.update - 'shopItemCollection' can not be null");
        }
        this.getHibernateTemplate().update(shopItemCollection);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<ShopItemCollection> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (ShopItemCollection entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(ShopItemCollection shopItemCollection)
    {
        if (shopItemCollection == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.remove - 'shopItemCollection' can not be null");
        }
        this.getHibernateTemplate().delete(shopItemCollection);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.remove - 'id' can not be null");
        }
        ShopItemCollection entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<ShopItemCollection> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "ShopItemCollection.remove - 'entities' can not be null");
        }
        this.getHibernateTemplate().deleteAll(entities);
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>ShopItemCollectionDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link ShopItemCollectionDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see ShopItemCollectionDao#transformEntity(int, ShopItemCollection)
     */
    public Object transformEntity(final int transform, final ShopItemCollection entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case ShopItemCollectionDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case ShopItemCollectionDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see ShopItemCollectionDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link ShopItemCollection}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof ShopItemCollection)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return ShopItemCollection
     */
    protected ShopItemCollection toEntity(Object[] row)
    {
        ShopItemCollection target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof ShopItemCollection)
                {
                    target = (ShopItemCollection)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), ShopItemCollectionImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(ShopItemCollectionDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), ShopItemCollectionImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<ShopItemCollection> search(final Search search)
    {
        return (Set<ShopItemCollection>) this.search(ShopItemCollectionDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public ShopItemCollection searchUniqueName(final String name)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("name",name,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<ShopItemCollection> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueName(final int transform, final String name)
    {
        final ShopItemCollection entity=this.searchUniqueName(name);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    // spring-hibernate-dao-base merge-point
}